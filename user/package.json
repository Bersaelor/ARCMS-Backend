{
  "name": "transcode-video",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "run-local-lambda --file index.js --event tests/event.json",
    "deploy": "aws lambda update-function-code --function-name arn:aws:lambda:eu-central-1:338756162532:function:transcode-video --zip-file fileb://Lambda-Deployment.zip",
    "predeploy": "zip -r Lambda-Deployment.zip * -x *.zip *.json *.log"
  },
  "author": "Konrad Feiler",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.471.0"
  },
  "devDependencies": {
    "run-local-lambda": "^1.1.1"
  }
}
